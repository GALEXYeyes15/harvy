# Copy to `.env.local` for local development (not committed).
OPENAI_API_KEY=

# Optional: absolute base URL for proofread API when not same-origin (e.g. Tauri production build).
# VITE_PROOFREAD_API_BASE=
