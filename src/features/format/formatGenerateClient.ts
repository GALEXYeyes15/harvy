/**
 * @deprecated Import from `./generation/twitterGeneration` instead.
 * Kept for compatibility — re-exports the Twitter generation client.
 */
export {
  requestTwitterFormatGeneration,
  type TwitterFormatGenerateRequest,
} from "./generation/twitterGeneration";
